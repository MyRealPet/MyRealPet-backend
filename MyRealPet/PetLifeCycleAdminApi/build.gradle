plugins {
    id "org.springframework.boot"
}

apply plugin: 'war'

dependencies {
    implementation(project(":PetLifeCycleCore"))
    implementation(project(":PetLifeCycleClient"))
    implementation(project(":PetLifeCycleDto"))
}
